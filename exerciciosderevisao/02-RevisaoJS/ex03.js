// Exercícios 3 - Variáveis

// Crie as variáveis:

// produto
// preco
// quantidade

// Mostre no console usando concatenando as variáveis:
// Meu (produto) custa (preco) e temos (quantidade) no estoque

let produto = "banana"
let preco = 200
let quantidade = 1
console.log("minha %s custa %d e temos %d no estoque", produto,preco,quantidade)

